# Copy to .env and fill in.
#
# Only pipeline/scrape_squads.py and pipeline/rescrape_missing.py need this.
# The processed data is committed, so you can run everything in models/
# without a key.
#
# Token for the NRP Nautilus OpenAI-compatible endpoint
# (https://ellm.nrp-nautilus.io/v1), used for LLM-based HTML extraction.
NRP_API_KEY=
